/** Internal constants. Not exported from the public barrel. */

/** Initial per-slot capacity for a fresh {@link EntityStore}. */
export const DEFAULT_ENTITY_CAPACITY = 1024;

/** Multiplier applied when a dynamic `Uint32Array` needs to grow. */
export const GROWTH_FACTOR = 2;
